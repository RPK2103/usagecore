{{- if .Values.entitlementRuntime.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: usagecore-entitlement-runtime
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "usagecore.appLabels" (dict "name" "entitlement-runtime" "component" "application" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.entitlementRuntime.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: entitlement-runtime
      app.kubernetes.io/instance: {{ .Release.Name }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: {{ .Values.rollingUpdate.maxUnavailable }}
      maxSurge: {{ .Values.rollingUpdate.maxSurge }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: entitlement-runtime
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: application
        app.kubernetes.io/part-of: usagecore
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      securityContext:
        {{- include "usagecore.podSecurityContext" . | nindent 8 }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: entitlement-runtime
                    app.kubernetes.io/instance: {{ .Release.Name }}
                topologyKey: kubernetes.io/hostname
      containers:
        - name: entitlement-runtime
          image: "{{ .Values.images.entitlementRuntime.repository }}:{{ .Values.images.entitlementRuntime.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          securityContext:
            {{- include "usagecore.containerSecurityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.entitlementRuntime.port }}
          env:
            - name: SERVER_PORT
              value: {{ .Values.entitlementRuntime.port | quote }}
            {{- include "usagecore.dbEnv" . | nindent 12 }}
            {{- include "usagecore.commonEnv" . | nindent 12 }}
          {{- include "usagecore.probes" (dict "port" .Values.entitlementRuntime.port "Values" .Values) | nindent 10 }}
          resources:
            {{- toYaml .Values.entitlementRuntime.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: usagecore-entitlement-runtime
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "usagecore.appLabels" (dict "name" "entitlement-runtime" "component" "application" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: entitlement-runtime
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.entitlementRuntime.port }}
      targetPort: http
{{- end }}
