# Local kind cluster for UsageCore operability validation.
# One control-plane node + two workers when machine resources permit.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: usagecore-local
nodes:
  - role: control-plane
  - role: worker
  - role: worker
